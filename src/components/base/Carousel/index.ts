export { default } from './Carousel.component';
/**
 * Note: Even though trying to sync JS timeout and CSS animation timings could not be perfect,
 * potential inaccuracy is still better than overcomplicated handling of all the animation on JS side.
 */
export { SLIDE_CHANGE_ANIMATION_TIME } from './config';
