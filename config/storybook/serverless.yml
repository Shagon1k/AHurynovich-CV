service: ahurynovich-cv-components-library

frameworkVersion: '2'

provider:
  name: aws
  runtime: nodejs12.x
  region: eu-west-1

# you can overwrite defaults here
#  stage: dev

custom:
  client:
    bucketName: ahurynovich-cv-components-library-s3-bucket
    # [other configuration parameters]
    distributionFolder: storybook-static
    indexDocument: index.html

plugins:
  - serverless-finch
